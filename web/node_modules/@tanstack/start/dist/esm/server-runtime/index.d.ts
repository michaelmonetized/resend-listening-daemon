import { Readable } from 'node:stream';
/**
 *
 * @returns {import('node:http').IncomingMessage}
 */
export declare function createIncomingMessage(url: string, method: string, headers: HeadersInit): Readable;
export declare function createServerReference(_fn: any, id: string, name: string): ((...args: Array<any>) => Promise<any>) & {
    url: string;
    filename: string;
    functionId: string;
};
